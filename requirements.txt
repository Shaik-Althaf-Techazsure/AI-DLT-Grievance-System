# --- CORE APPLICATION ---
Flask==3.1.2
gunicorn==23.0.0
Werkzeug==3.1.3
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3

# --- DATABASE & ORM (Assuming PostgreSQL for Render) ---
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.44
psycopg2-binary # Use the binary version to avoid compilation errors on Render
greenlet==3.2.4 # Required by SQLAlchemy

# --- EXTERNAL API & AUTHENTICATION ---
requests==2.32.5
urllib3==2.5.0
idna==3.11
certifi==2025.10.5
charset-normalizer==3.4.4
PyMySQL==1.1.2 # Retained for potential local development fallbacks if needed
passlib # Used by Flask for password hashing

# --- UTILITIES & SECURITY ---
click==8.3.0
setuptools==80.9.0
wheel==0.45.1
six==1.17.0
typing_extensions==4.15.0
Pillow==12.0.0 # Pillow is required for image handling (Base64 conversion)
